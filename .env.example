NODE_ENV=production
PORT=6100
HOST=0.0.0.0
CHESS_DB_FILE=/app/data/chess.db

# Optional: Redis URL for distributed pub/sub (multi-instance deployments)
# Leave unset for single-instance mode (uses in-process EventEmitter)
# REDIS_URL=redis://localhost:6300
